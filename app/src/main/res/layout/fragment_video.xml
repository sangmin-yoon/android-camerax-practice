<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/ic_fire_solid"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="10dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:layout_constraintBottom_toTopOf ="@id/video_capture_button"
        android:background="#4D000000"
        android:textColor="@color/white"
        android:layout_marginBottom="20dp"
        android:textSize="25sp"
        android:padding="20dp"
        android:text="test 테스트 입 니 다 ....  .다 다다다다ㅏ다닫다다다다다"
        tools:text="test 테스트 입 니 다 ....  .다 다다다다ㅏ다닫다다다다다"/>

    <ImageButton
        android:id="@+id/ib_prompt_btn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/ic_images_solid"
        app:layout_constraintEnd_toStartOf="@id/video_capture_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/video_capture_button"
        app:layout_constraintTop_toTopOf="@id/video_capture_button"/>

    <ImageButton
        android:id="@+id/ib_select_btn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/ic_align_right_solid"
        app:layout_constraintStart_toEndOf="@id/video_capture_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/video_capture_button"
        app:layout_constraintTop_toTopOf="@id/video_capture_button"/>



    <ImageButton
        android:id="@+id/video_capture_button"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:elevation="2dp"
        android:background="@drawable/camera_btn"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>